#include <bits/stdc++.h>
#define fi first
#define se second
#define all(x) (x).begin(), (x).end()
typedef long long ll;

using namespace std;

int main() {
  try {
    freopen("nameIn.txt", "r", stdin);
  } catch(const std::exception& e) {
    cerr << e.what() << '\n';
  }
  
  
}